# Todo app (with user accounts) using MongoDB

## Dependencies

ImageMagick 6.4.9+

To install this on EY Cloud, run the following:

    gem install engineyard-recipes
    ey-recipe init
    ey-recipes recipe imagemagick -p "media-gfx/imagemagick" -v "6.5.1.0" -u
    ey recipes upload --apply

